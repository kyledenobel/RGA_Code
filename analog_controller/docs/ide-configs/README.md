# Setting up IDE

Copy your IDE's corresponding settings folder into the top-level directory of the repository

## VScode

Copy the `vscode` folder into the top-level directory and rename to `.vscode`

### Recommended extensions for VScode

- C/C++ Extension Pack
- WSL (if Windows)
- Makefile Tools
- learn-markdown
    - Provides markdown preview inside IDE